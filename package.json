{
  "name": "nivano-search",
  "version": "1.0.0",
  "description": "App para buscar clientes no Google Maps gratuitamente",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
